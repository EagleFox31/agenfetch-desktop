name: Repository governance

on:
  workflow_dispatch:
    inputs:
      governance_mode:
        description: Preview with plan or reconcile with apply.
        required: true
        default: plan
        type: choice
        options:
          - plan
          - apply
  push:
    paths:
      - .github/project-config.json
      - .github/workflows/repository-governance.yml
  schedule:
    - cron: '17 3 * * *'

permissions:
  contents: read

jobs:
  reconcile:
    if: github.event_name != 'push' || github.ref_name == github.event.repository.default_branch
    uses: EagleFox31/appfactory-project-automation/.github/workflows/reusable-repository-governance.yml@1a5e2b3e996cff77c631235c6dfd42ca710eecb1
    with:
      governance_mode: ${{ github.event_name == 'workflow_dispatch' && inputs.governance_mode || 'apply' }}
      config_path: .github/project-config.json
      appfactory_ref: 1a5e2b3e996cff77c631235c6dfd42ca710eecb1
    secrets:
      governance_token: ${{ secrets.APPFACTORY_GOVERNANCE_TOKEN }}
